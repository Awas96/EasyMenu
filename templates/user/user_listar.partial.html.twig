<div class="tabla_fondo">
    <h2 class="text-center texto_mas_grande p-3">
        <i class="fas fa-user"></i>
        <div>
            <span class="texto_grande">Usuarios</span>
        </div>
    </h2>
</div>
<hr>
<div class="tabla_orden tabla_fondo">
    <table class="tabla table table-borderless text-center" style="table-layout: fixed">
        <thead class="tabla_cabecera">
        <tr>
            <th>Nombre de Usuario</th>
            <th>Rol</th>
            <th>Opción</th>

        </tr>
        </thead>
        <tbody class="texto_grande">
        {% for i in usuarios %}
            <tr>
                <td> {{ i.username }}</td>
                <td>{{ i.roles[0] }}</td>
                <td>

                    {% if estado == 1 %}
                        <button class="btn btn-primary">Promocionar</button>
                    {% else %}
                        <button class="btn btn-primary">Modificar</button>
                    {% endif %}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>

